<h1>Edit  Wiki</h1>

<div class="row">
  <div class="col-md-4">
    <p>Guidelines for wikis</p>
    <ul>
      <li>Please ask away.</li>
      <li>No need to do your own research first - we're happy to help!</li>
      <li>Seriously, though. You know what to do.</li>
    </ul>
  </div>
  <div class="col-md-8">
    <%= render partial: 'form', locals: { wiki: @wiki }  %>
  </div>
</div>
<div class="row">
  <% if @wiki.private? && (current_user.role == "premium" || "admin")  %>
    <div class="col-md-4">
      <%= render partial: 'collaborators/list' %>
      <%= render partial: 'collaborators/form', locals: { wiki: @wiki } %>
    </div>
  <% end %>
</div>
